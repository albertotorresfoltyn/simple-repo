{
  "name": "simple-repo",
  "version": "1.0.1",
  "description": "Simple repository pattern implementation. Basically a CRUD based on mongo driver and node",
  "main": "./distribution/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel source --presets babel-preset-es2015 --out-dir distribution",
    "prepublish": "npm run build"
  },
  "keywords": [
    "node",
    "mongo",
    "repository",
    "ecma",
    "6"
  ],
  "author": "Alberto Torres",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
